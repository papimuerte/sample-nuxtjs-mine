spec:
  name: sample-nuxtjs
  services:
    dockerfile_path: Dockerfile
  - build_command: yarn build
    environment_slug: node-js
    git:
      branch: main
      repo_clone_url: https://github.com/papimuerte/sample-nuxtjs-mine.git
    name: sample-nuxtjs
