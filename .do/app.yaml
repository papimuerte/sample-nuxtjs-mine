name: sample-nuxtjs
services:
  dockerfile_path: Dockerfile
- build_command: yarn build
  environment_slug: node-js
  github:
    branch: main
    deploy_on_push: true
    repo: papimuerte/sample-nuxtjs-mine
  name: sample-nuxtjs
